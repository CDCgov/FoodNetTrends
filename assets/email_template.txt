Run Name: $runName

<% if (success){
    out << "## CDCgov/FoodNetTrends execution completed successfully! ##"
} else {
    out << """
## CDCgov/FoodNetTrends execution completed unsuccessfully! ##
The exit status of the task that caused the workflow execution to fail was: ${exitStatus}
The full error message was:

${errorReport}
"""
} %>

<% if (verbose){
    out << "## Workflow execution summary ##"
    out << """
    Script:        ${workflow.scriptFile}
    Params:        ${workflow.profile}
    Command line:  ${workflow.commandLine}
    Completed at:  ${workflow.complete}
    Duration:      ${workflow.duration}
    Success:       ${workflow.success}
    Exit status:   ${workflow.exitStatus}
    Error report:  ${workflow.errorReport ?: '-'}
"""
} %>

The workflow was completed at $dateComplete (duration: $duration)

The command used to launch the workflow was as follows:

  $commandLine



Pipeline Configuration:
-----------------------
<% out << summary.collect{ k,v -> " - $k: $v" }.join("\n") %>

--
CDCgov/FoodNetTrends
https://github.com/CDCgov/FoodNetTrends

<% if (email) { %>
Sent by ${email}
<% } %>
